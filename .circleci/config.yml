version: 2.1

jobs:
  test:
    docker:
      - image: cimg/node:14.16
    steps:
      - checkout
      - run: npm install
      - run: npm test
      - run: npm run build
      - run: chmod a+x deploy.sh
      - run: ./deploy.sh
  build:
    docker:
      - image: cimg/node:14.16
    steps:
      - checkout
      - run: npm install
      - run: npm test

workflows:
  version: 2.1
  test:
    jobs:
      - test
#      - build
